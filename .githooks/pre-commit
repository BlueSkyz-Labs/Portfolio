#!/usr/bin/env sh
set -eu

printf '%s\n' 'Running local engineering gates (architecture, typecheck, lint, format, build, client budget)...'
pnpm test:architecture
pnpm typecheck
pnpm lint
pnpm format:check
pnpm build
pnpm check:client-budget
pnpm check:static-links
printf '%s\n' 'Local source gate passed. Full Playwright/Lighthouse remain promotion/preview evidence.'
