name: refresh-react-types-lockfile

on:
  push:
    branches:
      - chore/react19-types-alignment

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chore/react19-types-alignment
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Regenerate lockfile from package manifest
        run: pnpm install --lockfile-only --no-frozen-lockfile

      - name: Remove one-shot workflow and commit generated lockfile
        run: |
          rm .github/workflows/refresh-lockfile-react-types.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json pnpm-lock.yaml .github/workflows/refresh-lockfile-react-types.yml
          git diff --cached --exit-code && exit 1 || true
          git commit -m "chore(tooling): refresh React 19 type lockfile"
          git push origin HEAD:chore/react19-types-alignment
