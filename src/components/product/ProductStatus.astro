---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span
  class="inline-flex rounded-full border border-[var(--border-subtle)] px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[var(--action-primary)]"
>
  {label}
</span>
