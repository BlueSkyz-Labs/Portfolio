# BlueSkyz Labs Web — public environment template only. Never commit secrets.
# Leave empty locally. Production Builds require a real corporate https domain
# (not localhost, *.workers.dev, or example.com / RFC 2606 documentation hosts).

PUBLIC_SITE_URL=""
PUBLIC_CONTACT_EMAIL=""
PUBLIC_SECURITY_EMAIL=""
